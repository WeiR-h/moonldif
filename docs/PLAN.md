# 第一轮实施与边界

日期：2026-09-15。先完成可独立运行、可验证的原型，再评估公开发布条件。

1. 独立工程、工具链配置与来源记录。
2. 字节属性模型、物理行位置、内容与变更解析。
3. 内容及 add/delete/modify/moddn/modrdn 的写回。
4. 结构报告、CLI、失败输入与独立内容对照。
5. 有限 moonldap 模型映射验证及交付说明。

原型采用有上限的完整缓冲区，不处理无限流。默认只接受 version: 1，兼容模式可接受缺版本头。URL 仅保留引用，不读取；未解析值阻止完整通过。未知操作不静默忽略。

不是完整 DN/Schema 校验器，不连接真实企业目录。格式正确与导入成功分开陈述。第一轮不创建公开仓库、不发布包、不代替参赛者提交申报。

按真实开发阶段提交，不通过空提交或机械拆分凑提交数量。候选包名暂为 WeiR-h/moonldif，发布前再次核对命名空间与生态查重。
