# 0.7.0 三分钟展示建议

0:00–0:40：载入 examples/profiles/rules.json 与 exceeded.ldif。说明这是合成维护计划，最多允许每份 5 条整条删除，实际 6 条被拦截。

0:40–1:15：点击“定位越界”找到第六条。筛选不存在的 DN，列表为空但拦截仍存在；完整报告仍包含全部操作。

1:15–2:00：将限制改为 6，旧下载立即失效。重新检查后通过，下载配置与完整报告，用 CLI 复现同一规则和有效指纹。阈值只是示例，真实操作需自行审批。

2:00–2:30：打开 incomplete.ldif，显示退出 2 且保留已知超限。说明不完整结果不能导出整理文件；batch 按文件独立限制，不是批次聚合保护。

2:30–3:00：展示 mooncakes 新接口与双平台注册表验证，说明同一 MoonBit 核心复用于库、CLI、浏览器和 CI。文件离线处理，不连接 LDAP 服务器。此脚本不代表已录制视频或第三方试用。

# 0.6.0 历史展示建议

0:00–0:45：打开在线工作台，载入 `examples/complete-review/changes.ldif`。261 项审阅中搜索 `tail-risk`，定位第 201 项之后的整条删除；说明风险检查覆盖全部已解析记录。

0:45–1:20：删除末尾完整删除记录，旧报告立即失效；重新检查通过后导出整理文件。先搜索 `demo-259`，再下载完整报告，确认仍有 260 项，保留指纹、选项和位置，不输出原始属性值。

1:20–2:05：切换迁移核对，载入同目录 before / after，搜索 `demo-259` 并定位两侧原文。下载完整差异报告，强调筛选不缩小分析范围，精确 DN / 字节比较不代替 LDAP 语义。

2:05–2:40：展示 mooncakes 0.6.0 的 ReviewSession / SnapshotSession / PageQuery 和 CLI 分页、`--all`。同一 MoonBit 核心服务库、CLI 与浏览器；展示 Windows / Ubuntu 注册表消费和公开双浏览器证据。

2:40–3:00：说明 32 MiB 完整报告上限、离线处理和不完整结果的明确状态。所有演示用合成数据。本文件是录制建议，个人验收和实际演示视频仍待完成。

# 0.5.0 历史展示建议

0:00–0:30：预检整条删除，定位风险。说明文件不上传，风险处理与报告由 MoonBit 核心生成。

0:30–1:20：迁移核对合成示例，找出邮箱缺失、成员值减少。输入 alice 筛选并定位；下载后指出报告仍有完整的 4 项已报告差异。

1:20–2:05：打开 volatile-before / volatile-after 示例，默认 2 项差异。排除 modifyTimestamp 后重新核对，仍保留邮箱变化；展示报告的规范化排除项、命中次数与源指纹。说明不能排除 DN、不能绕过不完整输入。

2:05–2:40：展示注册表安装及 compare_snapshots API，说明相同核心可嵌入 MoonBit 应用、CLI 和工作台。展示独立模型、跨平台与浏览器证据。

2:40–3:00：陈述离线边界及真实完成状态；不宣称 Schema / 服务端验证或真实企业使用。此文是录制建议，不是已录制视频或答辩证明。

## 历史 0.4.0 脚本

0:00–0:35：打开工作台，文件预检的整条删除示例展示风险拦截及位置。

0:35–1:35：切换“迁移前后核对”，载入合成迁移示例并开始核对。指出 Alice 的 CN 只是 Base64 表示变化，没有误报；邮箱与成员缺失、Bob 缺失及 Carol 新增分别列出。定位迁移前邮箱源行。

1:35–2:10：下载核对报告，展示两份输入指纹、位置与明确比较边界。编辑迁移后内容，证明旧报告立即不可下载，重新核对后生成新结果。

2:10–2:45：展示 compare_snapshots 公共 MoonBit API 及 compare CLI，说明同一核心复用于浏览器和 CI；展示注册表独立安装与模型对照证据。

2:45–3:00：说明项目补充 MoonLDAP 的文件层，不连接服务器、不实现名称匹配规则、不生成执行补丁。演示都是合成文件，不冒充真实企业试用。

此文是录制脚本，不表示已录制视频或完成答辩。原有单文件脚本如下保留。

# 三分钟演示与个人复验（0.3.0）

演示入口：https://weir-h.github.io/moonldif/ 。展示页脚版本；全部示例均为合成资料。检查通过只表示支持范围和已启用策略通过，不代表真实服务器导入成功。

| 时段 | 操作 | 应看到的结果与解释 |
|---|---|---|
| 0:00–0:35 | 选择“整条删除”，点击定位原文 | 默认拦截删除，目标 DN 与位置可核对；LDIF 导出不可用 |
| 0:35–1:05 | 选择“属性清空”，启用拦截属性清空并重新检查 | 无值 replace / delete 被拦截；指定值删除有不同语义 |
| 1:05–1:35 | 为 replace: mail 补一行 mail: demo@example.com，删除整个 delete: description 子操作并重新检查 | 内容已修改时旧报告失效；检查通过后可导出新文件 |
| 1:35–2:05 | 下载 Markdown 或 JSON 报告，展示源指纹、选项、位置 | 没有原始属性值；包含 DN；指纹用于核对内容而非签名 |
| 2:05–2:30 | 选择“改名与移动”启用拦截，再选择“不完整输入” | 分别为策略拦截 1、不完整 2；两者仍可下载报告但不能导出 LDIF |
| 2:30–3:00 | 展示 mooncakes API 页面和以下调用 | 库可直接接入 MoonBit；CLI 和浏览器复用同一核心 |

```moonbit
let report = @ldif.check_text(
  "version: 1\ndn: cn=Demo\nchangetype: modify\nreplace: mail\n-\n",
  risk_policy={ deny_clear: true, deny_rename: false },
)
println(report.exit_code()) // 1
```

CLI 复验：`node dist/moonldif.js check <导出的文件>` 应返回 0，再用 `inspect --format json` 检查语义。PowerShell 用 `$LASTEXITCODE` 查看退出码。

参赛者独立执行以上流程并记录日期、浏览器、实际结果和问题；当前 AI 开发自测不得写成第三方试用。演示脚本是可录制材料，不表示已有演示视频或完成答辩。

## 批量预检展示（可替换演示最后 30 秒）

执行 README 的 batch 三文件示例，展示每份文件的基本名、指纹和退出码，以及整批返回 1。混入一个不存在的路径，再展示整批返回 2、已有风险仍保留。打开 examples/ci/README.md 解释如何让检查阻止有风险的 CI。批量能力在 CLI 和公共库中，网页仍为单文件操作。
