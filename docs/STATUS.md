# 当前交付状态

日期：2026-09-15。版本：0.1.0-dev.2。本地开发版；公开仓库、正式发布和赛事审核均未完成。

## 已实现并复验

- LDIF 内容与四类变更、属性字节、折行、源位置、确定性写回及删除策略。
- DN/RDN 字符串语法检查；明确启用的旧分隔空格模式，不自动改写名称。
- 同一套 26 个核心测试在 JS、Wasm GC 上分别通过；7 组 CLI 集成测试通过；三个场景符合 0、1、0 预期。
- Python 独立参考 8 组通过。Java SDK 27 组对照：26 组三方向直接通过；1 组 SDK 输出经扩展语法转换后比较通过，明确记录为差异。
- 7 个 RFC 示例的原刊与修订版本均符合已声明的预期；其中含外部引用的修订版仍为 incomplete，并非全部有效通过。
- moonldap 可选适配的 2 个测试复验通过，覆盖四种操作模型与 BER 往返；仍不连接服务器、不支持操作控制映射。
- 100～10,000 条记录的 8 组规模验证完成。修复写回校验的重复 JSON 树分配；保留优化前后读数。
- 本机格式、禁止警告的类型检查、构建通过。远端 Windows/Ubuntu CI 配置加入 Wasm GC、SDK 和 RFC 对照，尚未运行。

本轮证据见 `verification/2026-09-15-dev.2/`；第一轮记录保留在 `verification/2026-09-15/`。详细范围见规范覆盖表、名称检查、互操作和性能文档。未把旧快照当作以后版本的结果。

## 已发现与保留的限制

- SDK 的空控制值异常及整项 Base64 扩展单独记录；不通过删掉样例使报告变绿。
- RFC 原刊的部分示例有排版或折行问题，修订性质分别标明，不能把所有修订都称为官方已确认勘误。
- 当前写回大文件仍消耗数百 MiB 进程内存；有界完整缓冲区不是流式库。
- 不验证目录 Schema、DN 匹配语义、BER 断言语义、服务端状态/ACL 或真实导入结果。

## 竞争力目标尚未完成

1. 更直接的变更审阅及可行动预检、浏览器本地检查—定位—修订—导出工作台。
2. 浏览器真实交互及资源限制验证；独立第三方按文档试用。
3. 公开仓库、远端 Windows/Ubuntu CI、mooncakes 正式发布、干净工程安装复验。
4. 最新生态查重、补齐与 moonldap 的互补说明和演示材料；参赛者依官方要求理解并完成人工申报。
5. 真实授权样本、主办方初审、答辩或获奖均没有证据，不能标记完成。

官方说明和历年作品对标在 `docs/AWARD_TARGET.md`。这些工程结果是在提高竞争力，不能推导出获奖承诺。
