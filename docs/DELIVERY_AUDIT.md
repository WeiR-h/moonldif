# 交付总核对与恢复入口

核对日期：2026-09-15。代码检查点：`562331f`，版本 `0.1.0-dev.4`。目标仍是依官方说明与历年优秀作品提升实际竞争力；本文件不是获奖认证，也不把本地完成缩减为整个目标完成。

本次只核对证据与状态，没有重复执行已经通过且未发生相关变化的全部测试。dev.4 源码清单中的 82 个文件逐一重新计算 SHA-256，全部一致；工作树核对前干净。历史有 17 个非空开发提交，覆盖初始化、模型、解析、写回、CLI、独立对照、生态适配、边界修正、CI、文档、名称、性能、工作台、归档接入和上游样本回归。

| 目标要求 | 现有证据与结论 | 尚缺的证明 |
|---|---|---|
| 官方说明与历届项目对标 | `AWARD_TARGET.md`：2024、2025、2026 官方来源和公开项目核查；区分赛道及决赛候选与已获奖 | 后续规则变动需在实际改报时复查，不能预测奖项 |
| MoonBit 主实现、公共 API | `src/` 实现文件模型、名称、诊断、写回和审阅；CLI/Worker 使用同一核心 | 正式注册表包的第三方安装 |
| 格式、字节、顺序和名称正确性 | dev.4 核心 JS/Wasm GC 各 31 测试；dev.3 Python/SDK/RFC 对照；`CONFORMANCE.md` 保留支持边界 | 全部 LDAP 扩展、Schema 和服务端执行不在当前范围 |
| 可用预检与错误阻断 | 9 组 CLI、三场景、未知操作回归；不完整优先退出 2，导出仍阻断 | 真实目录环境不能由文件检查代证 |
| 文件到定位、修订、导出体验 | dev.3 完整浏览器交互、实际下载模型比较；dev.4 针对分类修正的桌面/手机视口验证 | 独立使用者试用、其他浏览器/真机未测 |
| 公开来源样本与独立验证 | 六份固定 OpenLDAP 原始数据：四份配置兼容的三方向对照、两份明确拒绝；原始错误保留 | 不是全部上游测试集，也不是企业客户采用证据 |
| 性能与资源说明 | dev.2 的 100～10,000 条规模数据、优化前后内存读数；浏览器与 CLI 明示上限 | 高内存限制仍存在；不是流式实现或峰值内存认证 |
| 生态互补和许可 | `ECOSYSTEM.md`、`THIRD_PARTY.md`、Apache-2.0 LICENSE；moonldap 实际离线模型/BER 验证；浏览器构建附运行依赖许可 | 发布前最终确认公开 payload 与包元数据 |
| 可复现说明、场景及实现讲解 | README、SUPPORT、SCENARIOS、DEMO、ARCHITECTURE；本地发行归档从新工作区在两目标接入成功 | 注册表下载、第三方按 README 完整试用 |
| 公开仓库与双系统 CI | 工作流含核心、工作台、独立对照、上游样本、归档及生态适配步骤 | 本轮账号查询未找到 `WeiR-h/moonldif`；无 remote，无远端运行结果 |
| mooncakes 正式发布 | 本地 `moon package` 归档和 API 调用通过 | `moon search WeiR-h/moonldif` 未找到包；未发布、未注册表安装 |
| 人工申报、审核及奖项 | 提供事实与演示资料，未代替参赛者人工理解与申报 | 均没有主办方接受证据，不能标记完成 |

## 当前阻断与恢复顺序

公开发布确认卡仍未收到回答。自动审批拒绝了创建公开仓库并推送 MoonLDIF，理由是此前明确授权针对 MoonAPI Check，没有覆盖新项目的仓库及公开内容。该条件已经跨三个连续目标轮次存在；期间完成了发行接入验证、上游样本回归与真实缺陷修正。本次核对后，下一阶段必需的公开交付无法在没有授权的情况下继续。

不以重新运行本地测试替代公开验证，不再通过添加无具体需求的功能延后这个交付关口。整体目标标记为等待用户授权，未标记完成。

收到明确范围后从这里恢复：

1. 按授权范围创建/核实仓库，复查最终公开文件列表与许可证，提交现有开发历史。
2. 跑通 Windows/Ubuntu GitHub Actions，处理真实远端失败并保存运行链接和日志。
3. 如已授权 mooncakes，核实发布身份，补齐正式版本元数据，发布并从无本地库映射的新工程下载验证。
4. 更新实际链接、版本、校验值和交付状态；按公开 README 做外部试用，收集真实反馈，不自行冒充第三方。
5. 使用已有演示与事实资料，支持参赛者理解和人工改报；审核及评奖状态继续单独记录。

当前开发版及全部失败/通过记录保留，不需要重新开始开发。
