# 生态互补与来源核查

复查日期：2026-09-15。检索结果不能证明生态绝对空白，申报前需再次核查。

本轮实际执行 `moon search ldif` 与 GitHub `ldif language:MoonBit` 仓库检索，均只得到 `bobzhang/ldiff`。它是代码词法差异工具，不是 LDIF 目录交换格式库；未据此宣称全球首创。

重点相邻项目：[hbYlj/MoonLDAP](https://github.com/hbYlj/MoonLDAP)，本轮 HEAD 仍为 `7475db606e5de118896a7c35a0c8c77583a87e59`。读取 README 和递归文件目录；README 介绍 LDAPv3、BER、DN、过滤器、会话与网络适配，未列出 LDIF 文件读写能力，目录路径检索也未命中 LDIF。未逐行审计上游全部实现，不能据此断言不存在任何未文档化能力。

| 能力 | 已有 MoonLDAP | MoonLDIF 的边界 |
|---|---|---|
| LDAP 协议、BER、网络会话 | 上游主体功能 | 核心不实现；可选适配复用上游模型 |
| DN 字符串解析 | 上游已有 | 本项目为文件源位置诊断实现有限语法检查；不将 DN 解析本身作为生态创新 |
| LDIF 内容和变更文件 | 本轮未发现文档入口 | 本项目核心：字节、折行、记录顺序、物理行位置 |
| 文件写回及复检 | 本轮未发现文档入口 | 原始字节和操作顺序核对；不保证目录服务端执行成功 |
| 文件操作审阅 | 上游提供操作模型 | 本项目从文件解释删除、清空、替换和移动，保留不完整诊断 |
| 本地文件工作台 | 上游 CLI 为 search/explain | 本项目检查、定位、编辑、复检、导出；不管理在线目录 |

互补链条：LDIF 文件 → MoonLDIF 字节/操作模型与离线诊断 → 可选模型映射 → MoonLDAP 操作模型。映射已做离线 BER 往返，未执行真实目录写入；控制项映射明确拒绝。原始 LDIF URL 值也不会读取。

申报应突出可复用文件层、互操作证据和连续使用流程，不使用“首个 LDAP 工具”“完整企业目录管理”“检查通过即可导入”等描述。完整许可证与独立参考来源见 `THIRD_PARTY.md`。
