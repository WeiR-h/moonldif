# 发行准备检查点

2026-09-15，0.1.0-dev.3，Windows 本机。

`package-consumer.json` 记录将本机 `moon package` 的归档解压到新工作区后，通过公共 API 完成 JS / Wasm GC 测试。调用工程仅使用归档中的库和安装好的工具链，没有引用开发源目录。此项是本地归档接入，不是 mooncakes 注册表安装。

`package-consumer-first-failure.json` 保留首次失败：测试调用工程缺少 moon.mod 的模块依赖声明。修正调用工程生成逻辑后，重新创建工作区并验证通过；核心实现未因此修改。

后续公开仓库、远端 CI、正式包和注册表安装分别记录。自动审批拒绝了新项目的公开创建/推送，正等待用户明确公开范围；未绕过审批。
